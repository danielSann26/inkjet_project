# Inkjet Scaffold Analyzer — dependencies
# Per Section 3 of the spec. PyTorch CPU-only (install separately via the
# index URL below to keep dependencies portable on machines without CUDA):
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# Python 3.10 or 3.11 required.

numpy>=1.24
opencv-python>=4.8
scikit-image>=0.21
scikit-learn>=1.3
PyQt6>=6.5
matplotlib>=3.7
Pillow>=10.0
